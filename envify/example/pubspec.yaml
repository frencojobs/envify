name: example
version: 0.0.1
description: An example for envify.

publish_to: none

environment:
  sdk: ">=2.8.1 <3.0.0"

dependencies:
  envify:
    path: ../

dev_dependencies:
  build_runner: ^1.10.0
  # envify_generator: ^1.0.0
